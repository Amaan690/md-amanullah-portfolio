import { education, metrics, skillGroups } from "@/data/portfolio";

export function MetricsStrip() { return <section className="signal-strip" aria-label="Key impact metrics">{metrics.map((metric) => <div className="metric" key={metric.label}><strong>{metric.value}</strong><span>{metric.label}</span></div>)}</section>; }
export function AboutSection() { return <section className="section about-section" id="about"><div className="section-label"><span>01</span><span>About the work</span></div><div className="about-grid"><h2>Software foundations.<br /><em>Model intuition.</em></h2><div className="about-body"><p className="lede">My path runs from Electronics &amp; Communication Engineering to software engineering, then deeper into AI/ML, LLM evaluation and AI platforms.</p><p>I bring the habits of a product engineer to model work: understand the system, define what good means, test the edges and make the result useful in production. That means full-stack delivery, backend systems, prompt engineering, model validation and clear technical feedback.</p><a className="text-link" href="#experience">See the journey <span aria-hidden="true">↘</span></a></div></div></section>; }
export function SkillsSection() { return <section className="section skills-section"><div className="section-label"><span>04</span><span>Toolkit</span></div><div className="skills-layout"><h2>The tools I use<br /><em>to make it real.</em></h2><div className="skill-groups">{skillGroups.map((group) => <div className="skill-group" key={group.title}><h3>{group.title}</h3><div className="tag-row">{group.items.map((skill) => <span key={skill}>{skill}</span>)}</div></div>)}</div></div></section>; }
export function EducationSection() { return <section className="section education-section" id="education"><div className="section-label"><span>06</span><span>Education</span></div><div className="education-grid"><h2>Always learning<br /><em>the next layer.</em></h2><div className="education-list">{education.map((item, index) => <div className="education-item" key={item.degree}><span className="education-index">0{index + 1}</span><div><h3>{item.degree}</h3><p>{item.field}</p><small>{item.institution}</small></div></div>)}</div></div></section>; }
export function SiteFooter() { return <footer className="site-footer"><span>© {new Date().getFullYear()} Md Amanullah</span><span>Portfolio content lives in <strong>data/portfolio.ts</strong></span><a href="#top">Back to top <span aria-hidden="true">↗</span></a></footer>; }
